<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".Game_Page"></activity>
        <!-- //zaqws manifest; -->
        <activity android:name=".myinformation" />
        <activity android:name=".myfighting" />
        <activity android:name=".page_4" />
        <activity android:name=".page_8" /> <!-- // JUNGSUNWOO manifest; -->
        <activity android:name=".HomePage" />
        <activity
            android:name=".activity_loading"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity android:name=".map_page_7" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB3EQtRgVN1hH3TWxFPptBfi0ekC-n53TE" />
    </application>

</manifest>